[tool.ruff]
line-length = 100
exclude = [
    ".pytest_cache",
    ".pytest_tmp",
    ".scannerwork",
    "data",
    "htmlcov",
    "logs",
    "outputs",
    "venv",
]

[tool.ruff.lint]
select = ["E4", "E7", "E9", "F"]

[tool.ruff.lint.per-file-ignores]
"src/agent/agent.py" = ["E402"]
